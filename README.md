# XAW DealerScan Pro (scaffold)

This repository is a starter scaffold for the DealerScan Pro application.

Structure:
- server/: backend services (VCI, OBD, CAN, DB)
- client/: Qt-based desktop client

See `requirements.txt` for Python dependencies.
